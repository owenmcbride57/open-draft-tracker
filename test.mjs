// Node entry point for `npm test`. The checks themselves live in tests.js, shared
// with the in-browser runner (tests.html). Importing the module runs every check
// and, under Node, sets a non-zero exit code if any of them failed.
import './tests.js';
